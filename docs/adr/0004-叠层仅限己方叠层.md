# ADR-0004 — 叠层仅限己方叠层(推翻"压顶接管")

初版实现允许把库存棋压顶到**敌方**同类叠层上(规则五.1 字面"叠层后最上层须为己方色"的推论),使敌方叠层被"接管"。规则作者确认此为误读:**叠层只能叠放到己方的同类叠层上**,目标格顶层必须已是己方色。改动点为 `moves.ts` 中叠层目标的格筛选(加顶层己方色条件);原"可压顶敌方同类叠层"测试改为"不可压顶敌方叠层"。此裁决与 ADR-0001(取层仅限己方)一致,叠层/取层操作都只作用于己方叠层。
